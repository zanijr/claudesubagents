{
  "$comment": "Oscar Wilson managed policy — OPTIONAL strict tier. Install to /etc/claude-code/managed-settings.json (Linux/WSL), /Library/Application Support/ClaudeCode/managed-settings.json (macOS), or C:\\Program Files\\ClaudeCode\\managed-settings.json (Windows). Rules here cannot be overridden by user, project, or CLI settings. Keep this file minimal: only the rules that must hold on every machine, in every repo.",
  "permissions": {
    "deny": [
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Bash(git push --force *)",
      "Bash(git push -f *)"
    ],
    "disableBypassPermissionsMode": "disable"
  }
}
